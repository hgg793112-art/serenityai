---
description: 需要打開網頁時用 MCP 操作；需登入時由使用者處理，再由 Agent 取得資訊並繼續後續操作
alwaysApply: true
---

# 網頁操作流程（MCP）

當任務需要**打開網頁、在瀏覽器裡操作或從網頁取得資訊**時，依下列流程執行。

## 1. 用 MCP 打開並操作

- 使用 **Chrome DevTools MCP**（如 `navigate_page`、`take_snapshot` 等）打開目標網址並進行可自動化的操作（點擊、填表、截圖、讀取頁面內容等）。
- 不要只給使用者「請手動打開某連結」的指示；由 Agent 透過 MCP 主動打開頁面並盡可能完成可自動化的步驟。

## 2. 遇到需要登入的頁面

- **先說明**：告知使用者「此頁需要登入，請在（已打開的）瀏覽器視窗中完成登入；完成後回覆我」。
- **暫停自動操作**：不在未登入狀態下替使用者輸入帳密或執行登入。
- **使用者完成登入後**：使用者回覆「已登入」或「好了」後，再透過 MCP：
  - 取得頁面上需要的資訊（例如 API Key、Project URL、anon key 等），或
  - 繼續執行後續操作（建立專案、複製金鑰、執行 SQL、點擊按鈕等）。

## 3. 取得資訊後的後續操作

- 將從網頁取得的資訊用於後續步驟（例如寫入 `.env.local`、更新設定檔、執行腳本等）。
- 若需使用者確認敏感資訊再寫入，可先列出「將寫入的內容」再執行寫入。

## 範例情境

| 情境 | Agent 行為 |
|------|------------|
| 取得 Gemini API Key | 用 MCP 打開 AI Studio；若需登入則請使用者登入，登入後用 MCP 導航到 API Key 頁、取得或協助複製 Key，再寫入 `.env.local`。 |
| 取得 Supabase URL / anon key | 用 MCP 打開 Supabase 專案設定；需登入則請使用者登入，登入後用 MCP 讀取 Project URL 與 anon key，再寫入 `.env.local` 並提醒執行 schema。 |
| 在 Supabase 執行 SQL | 用 MCP 打開 SQL Editor；若已登入則導航並貼上 SQL、執行，再回報結果。 |

**總結**：需要網頁就由 Agent 用 MCP 開頁操作；要登入時交給使用者處理，使用者完成後再由 Agent 取得資訊並繼續後續操作。
